{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Sriram Kukkadapu — QA Engineering Manager portfolio, tested with its own Playwright suite.",
  "scripts": {
    "start": "npx http-server . -p 4173 -s",
    "test": "playwright test",
    "test:summary": "node scripts/generate-summary.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "http-server": "^14.1.1"
  }
}
